I will redesign the **Orders Page (Siparişlerim Sayfası)** for the web to provide a seamless and responsive experience, matching the design of the Account Page.

### Implementation Plan

1.  **Refactor Sidebar (Navigation Menu)**
    -   Extract the existing sidebar from `AccountPage` into a reusable widget `AccountSidebar` (`lib/widgets/account_sidebar.dart`).
    -   This allows us to show the same sidebar on the Orders Page, ensuring users can easily navigate between their account sections without losing context.

2.  **Update Account Page**
    -   Refactor `AccountPage` to use the new `AccountSidebar` widget.

3.  **Redesign Orders Page (`lib/screens/orders_page.dart`)**
    -   **Responsive Layout:** Implement a `LayoutBuilder` to switch between the mobile view (existing design) and a new desktop/web view.
    -   **Web Layout Structure:**
        -   **Header/Footer:** Integrate `WebHeader` and `WebFooter`.
        -   **Split View:** Use a 2-column layout:
            -   **Left:** `AccountSidebar` (active item: 'Siparişlerim').
            -   **Right:** The main orders content.
    -   **Content Enhancements:**
        -   Add a clear page title ("Siparişlerim").
        -   Optimize the Search Bar and Filter buttons for desktop width.
        -   Ensure the Tab Bar (All, Continuing, Delivered) looks good on larger screens.
        -   Reuse the `_buildOrderCard` but ensure it renders correctly within the split view container.

### Expected Outcome
-   When you click "Siparişlerim" on the web, you will see a full-page web interface.
-   The sidebar will remain visible on the left.
-   The orders list will be displayed cleanly on the right.
-   The design will be consistent with the "Hesap Özeti" (Account Summary) page shown in your screenshot.