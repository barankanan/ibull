I will refactor the `ProductCard` widget in `lib/widgets/product_card.dart` to use `InkWell` instead of `GestureDetector`. This will fix the navigation issue on mobile by ensuring reliable touch handling and providing visual feedback (ripple effect).

**Changes:**

1. **Extract Navigation Logic:** Move the `Navigator.push` logic to a `_onCardTap` method.
2. **Refactor** **`_buildNormalCard`** **&** **`_buildCompactCard`:**

   * Wrap internal content in `Material` and `InkWell`.

   * Move `color: Colors.white` from the container decoration to the `Material` widget.

   * Ensure `ClipRRect` is used to clip the InkWell ripple to the card's rounded corners.

