I will implement the redesign of the **Product Reviews Section** (`product_reviews_section.dart`) to match the visual reference you provided.

**Implementation Steps:**

1. **Update Layout Structure:**

   * Change the section title to **"Değerlendirme ve Yorumlar"**.

   * Create a **Rating Summary** widget containing:

     * Product thumbnail image on the left.

     * 5-level progress bars (5 stars to 1 star) with counts on the right.

2. **Add Statistics & Actions:**

   * Add a row showing **"580 Kişi"** (Reviewer count) and **"5.0"** (Average rating) with stars.

   * Implement the **"312 Değerlendirme Tümünü Gör"** button with the specific purple outline style.

3. **Redesign Review Card:**

   * Update the card layout to include:

     * **Header:** User name (e.g., "Baran K\*\*\*") and Date.

     * **Content:** Review text.

     * **Footer:** Rating (5.0), Chat icon button, and Review Images preview (with +count indicator).

4. **Save Changes:**

   * Apply all these changes to `lib/widgets/product_detail/product_reviews_section.dart` and save the file.

