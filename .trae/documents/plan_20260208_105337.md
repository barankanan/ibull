I will redesign the `CartPage` to support a responsive web layout that matches your design.

**Implementation Plan:**

1.  **Layout Structure Update**:
    -   Modify the `build` method in `lib/screens/cart_page.dart` to use `LayoutBuilder`.
    -   If the screen width is greater than 900px, display the new `_buildWebLayout`.
    -   Otherwise, keep the existing mobile layout.

2.  **Web Layout Implementation (`_buildWebLayout`)**:
    -   Create a two-column structure using `Row` (Left 70%, Right 30%) or `Flex`.
    -   **Left Column**:
        -   **Coupons Section**: "İndirim Kuponlarım" header and a horizontal list of available coupons.
        -   **Product List**: Iterate through stores and products with a new design.
            -   **Store Header**: Checkbox, Store Name, Rating (9.1 style), Navigation Arrow.
            -   **Shipping Banner**: "Kargo Bedava!" green banner.
            -   **Product Card**: Wide layout with Checkbox, Image, Title/Details, Quantity Counter (+/-), Delete Icon, Price (Old/New/Savings).
    -   **Right Column**:
        -   **Banner**: "1 TL ile yapabileceğin..." (Image placeholder).
        -   **Summary Card (Sepet Özeti)**:
            -   Subtotal, Shipping Cost.
            -   "Toplam Kazancın" (Savings).
            -   Total Price.
            -   "İndirim Kodu Gir" button.
            -   "Sepeti Onayla" (Checkout) button (Orange).
        -   **Info Card**: "En yakın gel al..."

3.  **New Widgets & Helpers**:
    -   `_buildWebProductRow`: A specialized row widget for the desktop product item view.
    -   `_buildWebSummaryCard`: The right-side summary box.
    -   `_buildWebCouponSection`: The coupon display section.

4.  **Logic Reuse**:
    -   Reuse existing `cartItems`, `totalPrice`, `_updateQuantity`, `_deleteProduct` logic to ensure functionality remains consistent.

This approach ensures the mobile app remains unchanged while the web version gets the specific design you requested.