import 'bulk_product_import_models.dart';

Future<BulkProductSelectedFile?> pickBulkProductCsvFile() async {
  throw UnsupportedError(
    'CSV dosya secme bu platformda desteklenmiyor. Web seller panelini kullanin.',
  );
}
